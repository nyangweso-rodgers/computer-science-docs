select 
    si.creation,
    si.modified,
    si.posting_date,
    si.posting_time,
    si.due_date,
    
    si.customer,
    si.customer_name,
    si.territory,
    si.name,
    si.status,
    si.return_against,
    si.amended_from,
    si.set_warehouse,
    si.set_target_warehouse,
    si.total_qty,
    si.base_total,
    si.base_net_total,
    si.total,
    si.net_total,
    si.discount_amount,
    si.base_grand_total,
    si.grand_total,
    si.total_advance,
    si.outstanding_amount,
    si.base_paid_amount,
    si.paid_amount
from `tabSales Invoice` si 