select 
        so.name,
        date(so.creation) as creation_date,
        so.creation as creation_datetime,
        date(so.modified) as modified_date,
        so.modified as modified_datetime,
        so.transaction_date,
        so.delivery_date,
        so.customer,
        so.customer_name,
        so.duka_latitude,
        so.duka_longitude,
        so.kyosk_status,
        so.workflow_state,
        so.kyosk_order_type,
        so.territory,
        so.order_type,
        so.docstatus,
        so.total_qty,
        so.base_total,
        so.base_net_total,
        so.total_net_weight,
        so.total,
        so.net_total,
        so.base_discount_amount,
        so.discount_amount,
        so.base_grand_total,
        so.grand_total,
        so.status,
        so.delivery_status,
        so.sales_partner,
        so.delivery_window_start_time,
        so.delivery_window_end_time,
        so.delivery_window_id,
        so.created_on_app
from `tabSales Order` so
order by 2 desc