syntax = "proto3";

message Person {
    required string first_name = 1;
    required int32 age = 2;
    optional string email_address = 3;
}
// Mandatory fields must be filled when a new message is constructed, otherwise, a runtime error will occur.